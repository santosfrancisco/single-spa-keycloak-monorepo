{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start",
    "bootstrap": "lerna bootstrap",
    "build": "lerna run build",
    "test": "lerna run test",
    "coverage": "lerna run coverage"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "6.0.1"
  }
}